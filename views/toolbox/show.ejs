<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Toolbox</title>
  </head>
  <body>
    <%- include("../partials/_navbar.ejs") %>

    <h1><%= userTool.tool.name %></h1>

    <% if (userTool.tool.description) { %>
    <p><%= userTool.tool.description %></p>
    <% } else { %>
    <p><em>No description available</em></p>
    <% } %> 
    
    <% if (userTool.tool.link) { %>
    <p>
      <a href="<%= userTool.tool.link %>" target="_blank"><button>More Info</button></a>
    </p>
    <% } %> 
    
    <% if (userTool.tool.reference) { %>
    <a href="<%= userTool.tool.reference %>" target="_blank"><button>Reference</button></a>
    <% } %>

    <% if (userTool.note) { %>
      <p>Notes: <%= userTool.note %></p>
      <% } %>


      <a href="/toolbox/myTools/<%= userTool._id %>/edit">
         <button>Edit Tool</button>
      </a>

 
   <form action="/toolbox/myTools/<%= userTool._id %>?_method=DELETE" method="POST">
         <button type="submit">Remove Tool</button>
      </form>

  </body>
</html>
